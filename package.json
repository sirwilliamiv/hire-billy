{
  "name": "hire-billy",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "description": "An interrogable candidate: one corpus, one nine-stage pipeline, three surfaces (browser UI, HTTP socket, MCP server).",
  "bin": { "hire-billy-mcp": "mcp/server.js" },
  "scripts": {
    "serve": "node serve.js",
    "mcp": "node mcp/server.js",
    "sync": "node scripts/sync-corpus.js",
    "build:summon": "node scripts/build-summon.js",
    "test": "node scripts/smoke.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  }
}
